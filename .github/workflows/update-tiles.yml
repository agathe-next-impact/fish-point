name: Update PMTiles (Western Europe)

# Reconstruit l'extrait Europe de l'Ouest des tuiles Protomaps depuis le planet PMTiles
# quotidien, puis pousse vers Vercel Blob.
#
# Déclencheurs :
#   - Cron mensuel (1er du mois à 03:00 UTC)
#   - Manuel via workflow_dispatch
#
# Secret GitHub requis :
#   - BLOB_READ_WRITE_TOKEN  (copier depuis Vercel dashboard → Project → Storage → Blob)
#
# Voir docs/ops/pmtiles-vercel-blob-setup.md pour le setup initial.

on:
  schedule:
    - cron: '0 3 1 * *'
  workflow_dispatch:
    inputs:
      planet_date:
        description: 'Date du planet PMTiles à utiliser (YYYYMMDD). Laisser vide pour la veille UTC.'
        required: false
        type: string

permissions:
  contents: read

jobs:
  build-and-upload:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22'

      - name: Install pmtiles CLI
        run: go install github.com/protomaps/go-pmtiles@latest

      - name: Resolve planet date
        id: planet
        run: |
          if [ -n "${{ inputs.planet_date }}" ]; then
            echo "date=${{ inputs.planet_date }}" >> "$GITHUB_OUTPUT"
          else
            # Yesterday UTC — the daily build is usually published with ~24h delay
            echo "date=$(date -u -d 'yesterday' +%Y%m%d)" >> "$GITHUB_OUTPUT"
          fi

      - name: Extract Western Europe from planet PMTiles
        run: |
          mkdir -p tiles
          # Western Europe without extra padding.
          pmtiles extract \
            "https://build.protomaps.com/${{ steps.planet.outputs.date }}.pmtiles" \
            tiles/western-europe.pmtiles \
            --bbox=-10.62,35.49,18.52,58.67
          ls -lh tiles/western-europe.pmtiles

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install deps
        run: npm ci --ignore-scripts

      - name: Upload to Vercel Blob
        env:
          BLOB_READ_WRITE_TOKEN: ${{ secrets.BLOB_READ_WRITE_TOKEN }}
        run: npm run upload-tiles -- ./tiles/western-europe.pmtiles --key=western-europe.pmtiles
